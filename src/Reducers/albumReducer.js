const initialState = []

const albumReducer = (state = initialState, action) => {
    switch(action.type) {
        case "ALBUMS":


        default: return state;
    }
}

export default albumReducer