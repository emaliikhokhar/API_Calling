const initialState = []

const commentReducer = (state = initialState, action) => {
    switch(action.type) {
        case "COMMENTS":


        default: return state;
    }
}

export default commentReducer